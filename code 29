#include <stdio.h>

int main() {
    int buffer = 0, choice;

    while (1) {
        printf("\n1. Produce\n2. Consume\n3. Exit\n");
        scanf("%d", &choice);

        switch(choice) {
            case 1:
                if (buffer == 0) {
                    buffer = 1;
                    printf("Item Produced\n");
                } else {
                    printf("Buffer Full\n");
                }
                break;

            case 2:
                if (buffer == 1) {
                    buffer = 0;
                    printf("Item Consumed\n");
                } else {
                    printf("Buffer Empty\n");
                }
                break;

            case 3:
                return 0;
        }
    }
}
